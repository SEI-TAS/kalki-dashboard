# This is the main configuration file for the application.
# https://www.playframework.com/documentation/latest/ConfigFile

# Default database configuration using PostgreSQL database engine
//db.default.driver=org.postgresql.Driver
//db.default.url="jdbc:postgresql://localhost:5432/kalki"
//#db.default.url="jdbc:postgresql://host.docker.internal:5432/kalki"
//db.default.username="kalki"
//db.default.password="kalki"
//db.default.jndiName=DefaultDS
//db.default.logSql=true

# db connections = ((physical_core_count * 2) + effective_spindle_count)
fixedConnectionPool = 9

database.dispatcher {
  executor = "thread-pool-executor"
  throughput = 1
  thread-pool-executor {
    fixed-pool-size = ${fixedConnectionPool}
  }
}

# TODO: Figure out how to do post requests with CSRF token
play.filters.disabled+=play.filters.csrf.CSRFFilter
